#include <bits/stdc++.h>
using namespace std;
int main() {
    string expression;
    cout << "Enter the expression(without spaces): ";
    cin >> expression;
    cout << expression << endl;
    return 0;
}
